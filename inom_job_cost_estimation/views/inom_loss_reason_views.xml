<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_inom_loss_reason_list" model="ir.ui.view">
        <field name="name">inom.cost.loss.reason.list</field>
        <field name="model">inom.cost.loss.reason</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="action_inom_loss_reason" model="ir.actions.act_window">
        <field name="name">Loss Reasons</field>
        <field name="res_model">inom.cost.loss.reason</field>
        <field name="view_mode">list</field>
    </record>

    <record id="view_inom_reject_wizard_form" model="ir.ui.view">
        <field name="name">inom.cost.reject.wizard.form</field>
        <field name="model">inom.cost.reject.wizard</field>
        <field name="arch" type="xml">
            <form string="Reject Estimate">
                <group>
                    <field name="estimate_id" invisible="1"/>
                    <field name="loss_reason_id"/>
                    <field name="loss_note" placeholder="What did the customer say?"/>
                </group>
                <footer>
                    <button name="action_confirm" type="object" string="Mark as Lost"
                            class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <menuitem id="menu_inom_loss_reason"
              name="Loss Reasons"
              parent="menu_inom_cost_config"
              action="action_inom_loss_reason"
              sequence="25"/>
</odoo>
